interface:
  display_name: "Qt 工程评审"
  short_description: "评审 Qt 工程体系、UI 边界与平台打包。"
  default_prompt: "结合 $review-engineering 的通用工程基线和 $review-qt 的 Qt 专项清单，对当前 Qt 项目做一次工程评审，并在 docs/reviews/ 下生成或合并评审报告。"

policy:
  allow_implicit_invocation: true
