interface:
  display_name: "Qt 工程评审"
  short_description: "评审 Qt 工程体系、UI 边界与平台打包。"
  default_prompt: "使用 $review-qt 先采用 $review-engineering 的通用工程基线，再读取 review-engineering/stacks/qt.md 的 Qt 专项清单，对当前项目做一次工程评审，并在 docs/reviews/ 下生成或合并评审报告。"

policy:
  allow_implicit_invocation: true
