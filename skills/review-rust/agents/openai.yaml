interface:
  display_name: "Rust 工程评审"
  short_description: "评审 Rust workspace、crate 边界与质量门禁。"
  default_prompt: "先按 $review-engineering 做通用工程评审，再使用 $review-rust 对当前 Rust 项目做专项工程评审，并在 docs/reviews/ 下生成或合并评审报告。"

policy:
  allow_implicit_invocation: true
